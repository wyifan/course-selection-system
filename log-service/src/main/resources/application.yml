spring:
  application:
    name: log-service
  datasource:
    url: jdbc:mysql://192.168.189.135:3306/learning?useSSL=false&serverTimezone=UTC
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      idle-timeout: 30000
      connection-timeout: 30000
mybatis-plus: 
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:  
    db-config:
      id-type: auto

server:
  port: 8709

#enable eureka client
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
  instance:
    lease-expiration-duration-in-seconds: 90
    lease-renewal-interval-in-seconds: 30

# springdoc:
#   api-docs: 
#     enabled: true
#     path: /api-docs #自定义 OpenAPI JSON 路径（默认 /v3/api-docs）
#   swagger-ui:
#     enabled: true
#     path: /swagger-ui-custom.html # 自定义 Swagger UI 路径（默认 /swagger-ui.html）
#   show-actuator: true # 如果需要显示 Actuator 端点
  
